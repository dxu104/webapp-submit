server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
      name: test
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/HW1db?createDatabaseIfNotExist=true

#      url: jdbc:mysql://localhost:3306/HW1db?createDatabaseIfNotExist=true
#      type: com.alibaba.druid.pool.DruidDataSource
      username: root
      password: root
mybatis-plus:
  global-config:
    db-config:
      table-prefix: "tbl_"
      id-type: ASSIGN_ID
  mapper-locations: classpath*:/mapper/**/*.xml,classpath*:com/gitee/sunchenbin/mybatis/actable/mapping/*/*.xml
mybatis:
  table:
    auto: update
  model:
    pack: com.csye6225HW1.POJO
  database:
    type: mysql

      #扫描用于创建表的对象的包名







#mybatis-plus-configuration
#mybatis-plus:
#  global-config:
#    db-config:
#      table-prefix: "tbl_"
#      id-type: ASSIGN_ID
#  mapper-locations: classpath*:com/gitee/sunchenbin/mybatis/actable/mapping/*/*.xml,classpath*:mapper/*/*.xml
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl





